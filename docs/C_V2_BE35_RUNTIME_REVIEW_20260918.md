# C：be35e5f固定批次复验与运行时契约检查

2026-09-18。**组合SHA `be35e5febe1acebce32ea9eb495affd6bd730875` + `c-delivery-20260917` 的C固定批次九组验收通过；运行时缓存/完整日线契约尚未通过，整体V2仍为Draft。** 本页不能作为实时Provider、真实LLM或浏览器放行依据。

## 已独立完成

- 新SHA在独立detached worktree检出，生产源码未改。12个C核心文件与85b7617一致；固定包六文件SHA-256与既有独立记录一致。
- 全量pytest **546 passed**，compileall通过；测试有1条既有AnyIO弃用提示。Python版本记录见checks证据。
- **MySQL 8.0.31**新建唯一隔离库，原项目数据库未改。三股各437行独立读回与normalized一致；真实V1→v8、中断恢复、重复迁移和旧列保全通过。
- 三股×三参数：完整包direct = B实际取出窗口direct；实际FastAPI ASGI POST的C字段与direct值/类型一致（五字段参数投影单独核对）；dispose并重建engine/session后实际GET的完整c_result、类型、输入哈希、订单、三条曲线一致。每曲线283点；新九组完整direct与旧85b7617结果一致。
- 历史详情/列表禁止解析回测服务仍成功，旧记录兼容。另C/D **7项**离线检查通过，验证默认AI与参数回测隔离及历史零外部重算；这7项使用SQLite、合成数据、假LLM。
- 腾讯派生单位定向检查：100→101得到0.01，首行无前值为null，裁剪后首行仍引用裁剪前有效收盘价；没有再次缩小100倍。

固定批次HTTP使用ASGI transport连接实际路由，市场输入来自冻结MySQL仓储适配器；不覆盖实时MarketDataService缓存策略，也不是TCP后端重启/浏览器测试。未把D自报的实时探测归为C独立实测。

| 股票/参数 | 收益率 | 订单 | 已完成往返 | 每条曲线点数 |
|---|---:|---:|---:|---:|
| 600519-default | -10.4591% | 18 | 9 | 283 |
| 600519-ma10-30 | -20.4053% | 14 | 7 | 283 |
| 600519-ma20-60-cost | -12.6368% | 7 | 3 | 283 |
| 000001-default | -4.5805% | 21 | 10 | 283 |
| 000001-ma10-30 | -6.2144% | 11 | 5 | 283 |
| 000001-ma20-60-cost | -3.6215% | 7 | 3 | 283 |
| 300750-default | 7.5449% | 18 | 9 | 283 |
| 300750-ma10-30 | 17.2570% | 12 | 6 | 283 |
| 300750-ma20-60-cost | 25.4583% | 5 | 2 | 283 |

## 未通过的运行时契约

独立合成数据/SQLite诊断复现以下情况，**不是实盘数据事故记录**。它们是前轮已提出的缓存与日期约定的实现缺口，不要求修改C算法。

1. **混源缓存及错误的整体来源**：先存旧来源1月6～7日，再同步腾讯1月8～9日；查询完整四日命中缓存，返回旧两行+新两行，provider却把全段标腾讯。当前读取stock_daily_sync.source仅修正单一批次来源，未解决整段混源。
2. **行情与成功来源非原子**：写成功来源时注入失败，重建会话仍读到新价格101，但来源仍是旧厂商，上次成功价格100没有恢复。需让整段行情替换与成功来源/范围同事务发布；失败可另记，不破坏上次成功状态。
3. **盘中完整日线口径未落地**：合成9月18日盘中场景，缓存到9月17日；日历按请求结束日计入18日而触发回源，提供含18日未完成行的响应后该行被接收。当前路径没有已完成交易日边界，也未筛除未完成行。

定向探针退出0只表示复现及断言完成，输出的 `contract_met=false` / `all_contracts_met=false` 才是契约结论；不能计成“三项问题已修复”。首次配置探针时漏用了项目SQLite BIGINT主键适配，已按tests/conftest.py补齐后重跑；这不属于产品缺陷。

前轮C已在 [5724159704](https://github.com/27ye/ai-quant-platform/issues/15#issuecomment-5724159704) 确认**方案A、暂不要求v9、按最近已完成交易日检查**。C口径无需再等确认；同一已完成区间要求不降低。按9月18日盘中示例，缓存只到9月16日依然缺9月17日。请D落实决策/实施归属并同步仍写“待C同意/放宽”的文档，B/D完成两类修复及相关回归后给新SHA，C再验受影响项。

## A的最新回复

已读取 [A 5726571532](https://github.com/27ye/ai-quant-platform/pull/13#issuecomment-5726571532)，独立核对 `8615d86db38ed3281e9bff4b101a32f6b6e6bc1d`：新提交仅改types/api.ts两处注释；与先前详情页label/注释合计三处均表达C输入快照SHA-256，字段路径/类型不变，**C此前提出的注释残留关闭**。本轮不重复构建纯注释改动，不代签A页面；A新视觉是否纳入本轮继续由D决定，不因修注释扩大集成范围。

## 证据与复现

[汇总](evidence/c-be35-20260918/summary.json) · [MySQL九组](evidence/c-be35-20260918/mysql-acceptance.json) · [27份完整对象哈希索引](evidence/c-be35-20260918/cross-path-evidence-index.json) · [源码/包/A核验](evidence/c-be35-20260918/source-audit.json) · [离线AI隔离](evidence/c-be35-20260918/AI-compatibility.json) · [定向诊断](evidence/c-be35-20260918/runtime-contract-probe.json)

在已安装依赖的Python环境中，将下列参数替换为同一SHA的独立检出目录和新输出文件路径：

```text
python docs/evidence/c-be35-20260918/probe_runtime_contract.py --source CANDIDATE_CHECKOUT --output NEW_PROBE_JSON
```

该探针针对be35e5f当前行为编写，HTTP使用替身、数据库为独立SQLite；不要把退出码用于最终验收通过判据。本轮提交仅C文档/证据/诊断脚本，未修改A/B/D代码，未合并或改变PR状态。
