# V2 AI 报告历史验收记录

最新复验：2026-09-22（下方保留前轮证据）

## 2026-09-22 D 最终独立验收与历史接口修复

- 验收从候选 `119a10c36c4b497ddfa0a3bb31666a6e6b803038` 开始。真实 API 复验发现：清空 LLM 配置后，历史列表与详情在 FastAPI 依赖解析阶段构造 LLM Client，导致只读请求返回 `50005`。提交 `c30f799` 将历史列表和详情切换到只依赖数据库的 `AIReportHistoryService`；生成接口继续使用完整 AI 依赖，不放宽 LLM 配置错误。
- 修复后的全量 `pytest tests -q` 为 **555 passed**；`compileall -q backend scripts`、C/D 历史兼容脚本、固定交付包一致性检查、前端 `vue-tsc --noEmit`、隔离目录 Vite 构建和 Git diff 空白检查均通过。固定包仍为三股各 437 行，三组 raw/normalized 均无结构或有效数值差异；Vite 仍只有既有的大包体提示。
- 独立 MySQL **8.0.41** 运行于 `127.0.0.1:3307`，迁移验收库 `ai_quant_v1_acceptance_20260922_002251_357713` 完成 V1→v8、旧报告保全和重复迁移检查。LLM 配置为空时，历史列表和旧报告 #1 详情均返回 200，旧记录保持 `legacy_missing`、`unknown` 和空快照；不存在报告返回 HTTP 404／`40006`；同进程的生成接口返回 `50005`，证明读写依赖已分离。
- 2026-09-21 同一候选链的真实 MySQL 故障探针在行情与来源整段替换期间注入 `SQLAlchemyError`，重连后仍保留旧收盘价 `[100.0, 101.0]`、来源 `eastmoney` 和 2 行记录，无部分提交。该修复之后未改动行情事务代码。
- 2026-09-21 实时探针结果：600519 与 000001 的 K 线分别返回 418 行和 257 行，参数回测均成功；300750 当前完整 K 线按契约拒绝为 HTTP 502／`50001`，固定窗口参数回测成功。三笔回测 ID 为 1、2、3，对应 C 输入快照哈希分别为 `ff26a496178036f241627f570f72759c64247be75ebb96f80c60ecd391f41e93`、`6f3b5ec3699fa71c8fef97c5131aea893cc4b1e1ec963b892db5dffb371444d3`、`fc03a520077a4632c771c767984dbcfff35ceaac8a35d7c10e635940ee0a981a`。
- 股票目录本轮首次同步仍受上游故障影响：主源超时，延迟源断开，空目录搜索正确返回 HTTP 503／`50006`。300750 未覆盖最近已完成交易日，因此实时三股发布门禁仍未通过，未以不完整数据代替成功。
- 真实 LLM 命令在发送前被自动审批拒绝：待发送内容包含 600519 行情、最多 10 条新闻、默认量化/回测指标和生成上下文，而实际 `.env` LLM 目的端未获明确授权。本轮没有绕过执行，也没有生成新的真实报告。
- 关闭 Mock 的前端已成功启动；原生浏览器对 `127.0.0.1:5173` 的访问被自动审批以 403 拒绝，页面操作未执行。前端类型检查和生产构建通过，但本轮浏览器流程仍记为未验收。
- PR #10 必须继续保持 Draft。当前剩余门禁为：上游目录恢复及 300750 完整实时数据、用户明确授权后的真实 LLM 验收、关闭 Mock 的浏览器流程，以及 B/C/A 在最终组合 SHA 上的复审和签字。

## 2026-09-21 最终运行时修复候选

- D 代码候选 `2c51b2e` 已落实 Issue #15 的三项运行时要求：切换来源时重新获取「既有缓存 ∪ 本次请求」完整窗口；日线与实际来源在同一事务内原子替换，失败回滚到上一成功快照；按 Asia/Shanghai 和交易日历将查询截止日裁剪到最近已完成交易日，盘中行不能用于凑完整性。
- B 最新契约来源为 `c7790c218487646298a3754ab0169063ba85e69e`；C 最新复核来源为 `7544862fa1956001275ee0e93cd4293cd6000e1d`，均以 merge commit 保留作者历史。A 的 `17b0ea3` 视觉升级按本轮范围决定不纳入 V2；已有页面继续保留「C 输入快照哈希」和冻结模式仅进入 600519 的限制。
- 新增回归覆盖：完整来源切换、来源元数据写入失败时整批回滚、盘中未完成日线过滤、交易日边界未知时保守失败、既有完整缓存与请求窗口的并集替换。全量 `pytest tests -q` 为 **554 passed**；`compileall -q backend scripts`、`scripts/validate_c_ai_history_compatibility.py`、固定包一致性工具均通过，三组 raw/normalized 各 437 行。前端 `vue-tsc --noEmit` 通过；Vite 使用 runner 配置加载并输出到隔离目录，2294 个模块构建通过，仅保留既有大包体提示。
- 2026-09-21 直连运行时 Provider：600519、000001 的 2025-09-01～2026-09-21 窗口分别返回 257 行，最新日为 2026-09-21；300750 返回 256 行，最新日为 2026-09-18。三者均实际使用腾讯端点。该探测证明兜底端点可达；300750 未覆盖当日，因此在生产完整性规则下不会被误标为完整快照。
- C 已在 `be35e5f` 上完成固定批次九组、POST 路径和新连接 MySQL 回读复验，并由此发现上述三个运行时缺口；这些缺口现已修复，但代码 SHA 已改变。合并门禁仍为：C 在最终组合 SHA 上复跑受影响探针和九组并签字；B 独立复审原子替换与数据来源；独立 MySQL 8 验证事务回滚；真实 LLM、实时三股链路及关闭 Mock 的浏览器流程复验。完成前 PR #10 保持 Draft。

## 2026-09-18 运行时腾讯 qfq 兜底：代码与数据复验

- D 代码提交 `0c66c7d`：仅当东财 K 线出现瞬时连接/超时故障时，改走腾讯 qfq；按不超过 730 天的窗口分块，保留前收盘价计算 `change_pct`，来源缺失的 `amount`、`turnover_rate` 保持 `null`。东财响应的必填金额校验不放宽；两源均失败仍为 `50001`。股票信息原有 `push2delay` 回退保留。
- AI 行情来源改为真实端点：本次实时查询取 `last_kline_source`，缓存查询取落库的 `stock_daily_sync.source`，以免腾讯行情被标作 `akshare`。本次测试覆盖 live/cache 来源；历史报告冻结快照的完整链路仍需最终环境复验。
- 本地全量测试 **546 passed**；`compileall -q backend scripts`、C/D 离线兼容、前端 `vue-tsc`/Vite 构建通过。前端输出使用隔离的 `.tmp` 目录，因既有 `frontend/dist` 与 `.vite-temp` 目录写入权限受限，未宣称默认 `npm run build` 通过。
- 直连运行时腾讯端点：600519、000001、300750 在 2025-09-01～2026-09-18 各返回 255 行，最新交易日 2026-09-17；600519 五年窗口分块返回 1210 行（2021-09-22～2026-09-17）。600519 与 `c-delivery-20260917` 的共同 437 行按规范字段逐项比较为 **0 差异**；该批次仍是独立的**交付验收数据**，不是运行时服务的成功证据。
- 待办门禁：在最终组合 SHA 上重跑独立 MySQL、三股并发冷启动、实时参数回测、真实 LLM 与完整浏览器流程；C 复核腾讯口径及九组结果，B 复审数据来源与并发修复，A 复验页面。B 在 Issue #15 指出的跨来源缓存混合及盘中完整性判据尚未定稿，不能视为已通过。PR #10 继续保持 Draft。

以下为 2026-09-17 的前一候选记录，其中实时 502 结论只描述当时的组合 SHA。

## 2026-09-17 候选：当时待实时行情复验与 A/B/C 签字

- 来源 SHA：B `540a00f604044bd046cd3935f4df9d018063b44b`（含 `c-delivery-20260917`）、C `87785788fed0a4f08bf5bd5b7a39a57010318666`、A `d7ba01c63eed07b23fff6939753d7ee591c140fb`。D 修复日历回归后用 merge commit 保留三人的提交历史；代码集成基线为 `cb3e9ad39d56fa2153f53175793f1f1554eed856`。最终签字使用本轮文档与验收辅助脚本提交后的完整 HEAD SHA，以 PR #10 最新提交为准。
- `TradingCalendarProvider(trade_dates=[...]).refresh()` 的 `TypeError` 已修复；静态日历刷新仍实际加载 AKShare，所有实际 AKShare 日历加载共用类级锁。静态刷新与并发首次加载回归测试通过。
- 最终组合全量 Pytest **542 passed**；`compileall -q backend scripts`、C/D 七项兼容、前端 `vue-tsc`/Vite 构建及 `git diff origin/main...HEAD --check` 通过。仓库没有 GitHub Actions 工作流，不将空的远程状态称为 CI 通过。
- B 的腾讯**交付验收数据** `docs/evidence/c-delivery-20260917/` 已合入并独立复核：六个 raw/normalized JSON 均与 SHA-256 清单一致，三股各 437 行，窗口为 2024-12-02～2026-09-16；C 固定窗口 2025-07-04～2026-08-31 的三股×三参数九组离线矩阵通过。该批次不是运行时 Eastmoney Provider 的替换：4 位小数 OHLC 与东财不逐值相同；源未提供的 `amount`、`turnover_rate` 保持 `null`，不作零值；`change_pct` 为派生值。
- 独立 MySQL **8.0.41**（本轮自建 `127.0.0.1:3308`）验证 V1→v8、v8 加列后中断恢复、重复迁移与旧报告保全，库 `ai_quant_v1_acceptance_finalchain_a6379d59`。冻结 R2 + 真实 LLM + MySQL 两份报告重启回读和哈希对账通过，库 `ai_quant_v1_acceptance_20260917_072317_494792`，报告 #1/#2、评分 33、403 行、历史 GET 的 LLM 调用 0；C/D 兼容检查另验证历史读取不调用行情、新闻与量化。
- 关闭全部 Mock 的最终组合浏览器验收使用隔离库 `ai_quant_v1_acceptance_20260917_073414_683543`：冻结 600519 工作台、参数回测（#2，2025-07-04～2026-08-31，收益 -10.46%）、真实 LLM 报告（#1，默认回测 -9.17%，未引用参数结果）、历史列表/详情、`C 输入快照哈希`、另一只股票的空列表、报告 404、明暗主题和冻结首页仅进入 600519 均通过。从 000001 返回报告 #1 后，页面正文与切换前逐字相同，哈希摘要仍为 `e74db05c…f3f96c35`。
- 实时目录同步成功 **5916 行**，浏览器搜索“宁德时代”命中 300750，新闻页返回 10 条真实新闻。但在本轮最终组合的新隔离库 `ai_quant_v1_acceptance_20260917_072619_193573` 中，三股 600519/000001/300750 的 K 线均返回 HTTP 502；实时 AI 报告独立尝试亦返回 HTTP 502／`50001`，库 `ai_quant_v1_acceptance_20260917_072357_543413`。因此**本轮实时多股票 K 线、实时参数回测和实时 AI 报告未通过**；此前 `4a7402a` 的实时成功记录仅作为前轮证据，不能替代最终 SHA 复验。
- 待办门禁：上游行情恢复后在最终 SHA 复跑三股并发冷启动、实时页面及真实 LLM；C 对最终 SHA 和固定数据批次独立签署九组结果；B 复审数据集成与 D 两处并发修复；A 复验实际页面。PR #10 在这些条件及用户最终验收齐备前保持 Draft；PR #12/#13/#14 仅保留来源记录。

以下均为前轮历史证据，不代表最终集成候选的当前通过状态。

## 2026-09-17 D 独立收尾

- 来源：A `010841099b20b500ed322b15cec32d9e7add97cd`、B `e4ef00060aef7a937c3086617256f9bfceda425a`、C `6beb1ab9246fd907602d4f056abf9b539f174bc3`。B/C 分别通过 `871baa7`、`4bf65eb` merge commit 合入，保留 B 两条新增恢复回归及 D 原回归。
- `e8f3380` 统一前端 C 输入快照哈希注释。`0fbd403` 修复冻结首页误沿用其他股票及首页固定 V1 文案：冻结入口固定到包内 600519，实时模式继续使用最近访问股票。
- 全量 **525 passed**；compileall、C/D 七项离线兼容、前端类型检查及构建通过。兼容证据在忽略的 `frozen/_runtime/cd_integration_20260917/`，使用合成行情、SQLite、假 LLM。
- 普通 `git diff origin/main...HEAD --check` 通过：B 的路径级 CRLF 属性保留原证据字节与哈希，不再需要全局 whitespace 覆盖。
- 真实 MySQL **8.0.41** V1→v8 通过，旧报告保留；新隔离库 `ai_quant_v1_acceptance_20260917_003108_169797`。
- 冻结 R2 + 真实 LLM + MySQL 复验通过：新隔离库 `ai_quant_v1_acceptance_20260917_003215_647578`，报告 #1/#2，评分33、403行、新闻0；重建应用后历史回读、API/数据库和哈希对账通过，历史 LLM 调用0。
- 浏览器后端已从候选代码重新启动，隔离库 `ai_quant_v1_acceptance_20260917_003350_249863`，前端关闭 Mock。实际生成报告 #1（deepseek-v4-flash），历史列表/详情、403行上下文、v2.0版本和哈希摘要 `0a2a1bfe…3a8daa48` 均可见。
- 1272px 宽页面检查了明暗主题及侧边栏；切换到冻结包外的000001后返回报告 #1，页面 main 的完整文字与切换前逐字相同。本项证明冻结报告不受路由切股影响，不替代实时多股票成功行情验收。
- 成功复现并验证首页修复：保存的最近股票为000001时，冻结首页入口改为进入600519。页面正常显示默认回测 -9.17%、12往返、24成交。
- 上游目录源已于本轮回恢复（push2/push2delay 均 ~0.3s 可达）：真实目录同步成功 **5916 行**（备用延迟源分页），实时搜索命中贵州茅台/宁德时代；C 三股（600519/000001/300750）各 2 并发冷启动 K 线全部 200、各 244 行，无 IntegrityError。期间发现并修复一个新的前置阻塞：AKShare 交易日历 fetch 内部构造 py_mini_racer V8 上下文，其一次性原生初始化非线程安全，并发首次加载直接整进程崩溃（FATAL partition_address_space）；修复为 `TradingCalendarProvider` 类级共享缓存 + 锁（顺带消除每请求重复拉取日历）。live 隔离库 `ai_quant_v1_acceptance_20260917_031511_189525`。
- 冷启动 K 线白卡已定位并修复：根因为首屏并发请求在新空库上并发 `sync_daily`，非原子 upsert 竞争同一批 `(stock_code, trade_date)` 键，后者 `IntegrityError 1062` → 50002 → 前端 catch 静默。修复为 `MarketDataService` 类级 per-stock 锁 + 持锁二次检查缓存（`refresh_snapshot` 刷新 REPEATABLE READ 快照）。post-fix 验证（新隔离库 `ai_quant_v1_acceptance_20260917_023937_348262`，schema v8，冻结 R2）：5 并发冷启动请求（4×kline + 1×detail）全部 200，仅 1 个线程执行 sync，无 IntegrityError，其余线程持锁复检命中 403 行缓存；用户浏览器复验确认不再复现。

### 本轮仍待完成

- B 新独立数据包、manifest、SHA-256 及 MySQL 回读，关闭旧包差异。
- 实时搜索与多股票链路 API 级已通过（目录 5916 行、三股并发冷启动正常）；浏览器页面级复验可在当前 live 后端上进行。
- C 基于最终 SHA 和新包的三股×三参数最终签字，以及 B/C 最终评审。

以下为历史轮次记录，状态和工具阻塞仅对应当时，不覆盖本轮结果。

## 最新组合：A/B/C 后续提交与 D 修复

- 来源：A `010841099b20b500ed322b15cec32d9e7add97cd`、B `25c11aa7b55d67fa5f77b3c603ca84b5369dc48e`、C `1d9cae17070dc7fe06a7c916ef8a0849e6790660`。三方历史均以 merge commit 保留。
- 当前数据库版本为 **v8**：AI 快照位于 v5，C 输入快照位于 v6，C 完整结果位于 v7，原文结果列 `c_result_text` 位于 v8。D 另修复了 v8 字段已添加但旧行尚未回填时的恢复路径。
- 默认 AI 报告继续使用默认量化配置；用户参数化回测保持独立，历史 GET 只读保存时的数据。

### 最新组合已验证

- 全量 `pytest tests -q`：**523 passed**；`compileall -q backend scripts` 通过。
- 前端 `npm run build`：类型检查及 Vite 构建通过，2294 个模块；仍有大于 500 kB 的产物提示。
- C/D 七项离线兼容检查通过，证据位于忽略的 `frozen/_runtime/cd_integration_20260916_084000/`；该检查使用 SQLite、合成行情和假 LLM。
- 独立 MySQL **8.0.41**（`127.0.0.1:3307`）：V1→v8 保留旧报告，验收库 `ai_quant_v1_acceptance_20260916_083440_026698`。
- 在同一隔离库模拟“v8 列已存在、版本仍为 7、旧 JSON 行未回填”：重新迁移补齐旧行、保留新行精确文本、记录 v8；再次执行结果不变。离线回归另覆盖该中断状态。
- 冻结 600519 + 真实 LLM + MySQL：验收库 `ai_quant_v1_acceptance_20260916_083607_545354`，生成报告 ID **1、2**，403 行、评分 33、新闻 0 条；重建应用后列表和两个详情回读通过，顺序为 2、1，历史 LLM 调用数为 **0**。API 与数据库报告及快照对账通过。
- 实时 600519 + 真实 LLM + MySQL：验收库 `ai_quant_v1_acceptance_20260916_083650_013523`，生成报告 ID **1**，模型 `deepseek-v4-flash`，schema v8；API 与数据库报告字段对账通过。
- 浏览器使用隔离库 `ai_quant_v1_acceptance_20260916_083728_578996`，前端关闭所有 Mock。新版暗色首页、600519 冻结工作台、K 线、评分 33 和浅色报告页已检查。初次自动回测显示失败，随后页面“重试”成功；再次进入工作台时默认回测正常。
- 浏览器提交参数化回测（2025-07-04 至 2026-08-31）保存为 #3，页面显示收益 -10.46%、往返 9 次、成交 18 笔；历史列表按 #3、#2、#1 排序，详情展示正式 `c_data_hash` 字段的摘要 `ff26a496…91f41e93`。
- 浏览器通过真实 LLM 生成 AI 报告 #1，打开历史列表与详情成功：评分 33、默认回测收益 -9.17%、冻结行情 403 行、行情截至 2026-08-31、版本均为 v2.0、上下文哈希摘要 `ec62d962…d3087b92`。默认 AI 内容未引用参数化回测收益 -10.46%。
- 浏览器已验证不存在报告的 404 页面、000001 报告空列表、未同步目录的“行情目录初始化中，请稍后重试”提示。切换到冻结包不包含的 000001 后显示无数据及回测失败状态；尚未完成返回原报告及实时多股票搜索流程，不能把本轮标记为完整浏览器验收。
- `git -c core.whitespace=cr-at-eol diff origin/main --check` 通过。直接对整条分支执行 `git diff origin/main...HEAD --check` 会将既有 CRLF 证据 JSON 的行尾报为尾随空格；没有为消除该提示改写历史证据文件。

### 尚未完成

- B 尚未发布重新导出的独立真实数据批次、manifest 和哈希；旧包的 600519 于 2026-09-15 的差异仍保留，不能宣称整包验收通过。
- C 尚未基于最终组合 SHA 重新签字；PR #10 的最终复审、桌面宽屏侧边栏和完整实时多股票浏览器流程仍待完成。
- 本轮目录同步命令第一次自动审批超时，重试及后续返回报告页操作被自动审批以模型容量不足为由拒绝，未绕过执行；因此成功目录搜索与跨股票返回原报告不计为本轮已通过。
- D 独立修复提交：`cd88b67`（v8 中断恢复）、`cc57482`（前端契约）；最新生产代码组合为 `cc57482`。后续文档提交不改变这一受测代码组合。
- 代码已推送至 PR #10，标题与描述已更新；远程 combined status 返回空列表，未声称远程 CI 通过。自动审批随后也阻止了本文件暂存、转 Draft 和 Issue #11 通知，当前 PR 实际仍为非 Draft，但不满足合并门禁；本文件修改尚未提交。

## 上一轮基线：`15315c6`（历史证据，非最新组合）

- B 行情与统一迁移：`85b9bdb`。
- C 参数化回测：`92df017`。
- A 报告历史页面：`d8af7b4`。
- D 分支使用 B 的统一迁移，当前 schema version 为 7；AI 快照字段位于 v5，D 不再维护独立 v2 迁移。
- 默认 AI 上下文继续使用默认量化配置，参数化回测结果未写入默认 AI 报告。

## 已通过

### 离线回归

- 全量 Pytest：477 项通过。
- `python -m compileall -q backend scripts`：通过。
- 前端 `npm run build`：通过，Vite 构建 2283 个模块；仅有既有的大包体提示。
- `git diff --check`：通过，无空白错误。
- C/D 兼容性脚本的七项检查通过，包括默认量化上下文不受参数化回测影响、哈希可复现、历史读取零外部调用和篡改快照拒绝。
- 覆盖生成、一次修复、失败回滚、快照哈希、分页排序、股票过滤、旧记录、部分元数据损坏和历史读取零外部调用。

### 真实 MySQL 8 与迁移

- 版本：MySQL 8.0.41，独立监听 `127.0.0.1:3307`。
- V1 → 当前版本增量迁移：通过，schema version 为 7，V1 旧报告保留。
- 冻结数据联合验收库：`ai_quant_v1_acceptance_20260916_025550_672253`。
- 两份报告 ID：1、2；列表排序、应用重建后的列表和详情回读均通过。
- 历史列表与详情期间 LLM 调用数：0。
- 旧 V1 报告返回 `snapshot_status=legacy_missing`、`source_mode=unknown`、`context_snapshot=null`。

### 冻结数据与真实 LLM

- 模型：`deepseek-v4-flash`。
- 股票：600519；行情行数 403；默认量化评分 33；`source_mode=frozen`。
- 生成两份真实报告成功，报告 ID 为 1、2。
- API 响应、MySQL 正文、版本字段、上下文快照和 SHA-256 哈希对账通过。
- 浏览器从工作台生成报告、打开历史列表和详情通过；详情显示冻结来源、403 行、版本 `v2.0` 和固定上下文哈希。

### 实时数据与真实 LLM

- 实时 API + MySQL 联合验收通过，验收库为 `ai_quant_v1_acceptance_20260916_025643_675431`。
- 600519 报告生成成功，报告 ID 为 1，模型为 `deepseek-v4-flash`。
- 浏览器实时页面加载了 600519 的行情、量化评分、默认回测和 10 条新闻。
- 实时页面第一次 AI 请求发生一次瞬时失败；同一隔离服务受控重试成功并保存报告。保存结果为评分 23、`source_mode=cache`、行情 244 行、新闻 10 条、报告 ID 1。

### 浏览器流程

- 冻结模式：搜索 600519 → 打开详情 → 页面生成报告 → 打开历史列表 → 查看报告 `#1`，通过。
- 实时模式：打开 600519 → 查看报告 `#1` → 切换到 000001 → 返回 `/ai/reports/1`，通过。
- 返回后原报告仍显示评分 23、贵州茅台正文、行情截至 2026-09-16、缓存来源、244 行上下文和原上下文哈希；000001 当前行情未覆盖历史内容。
- 历史报告列表未返回或展示完整 `context_snapshot`；详情页只读保存时的快照。

## 已知限制

- 实时股票搜索首次加载依赖上游全量股票列表，本次浏览器等待窗口内未返回结果；跨股票验收通过直接打开 000001 详情完成。股票详情和实时报告链路本身均可用。
- Vite 仍提示单个产物超过 500 kB；不影响本次功能验收。
