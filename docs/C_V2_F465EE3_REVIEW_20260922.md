# C：f465ee3 阶段复验（后续版本已取代）

本页是本轮较早阶段的真实记录；D 随后发布 bcbd559 并通过 C 复验，当前状态见 [bcbd559 验收报告](C_V2_BCBD559_ACCEPTANCE_20260922.md)。B 后续已另交 cb2cfeb，亦见新报告中的独立对比。

2026-09-22。被测完整组合 **`f465ee3b0e96c7787c6d5102da4f51455b76905d`**，固定数据批次 `c-delivery-20260917`。

**固定样本九组及新历史读取边界通过；两处运行时阻塞仍存在，整体 V2 暂不放行。**

## 本轮输入及实际差异

- [B 5769578187](https://github.com/27ye/ai-quant-platform/issues/15#issuecomment-5769578187) 已撤回“无阻塞”结论，确认 C 两处复现成立，并提出修复代码片段；B 明确没有提交实现，PR #14 仍 c7790c2，因此不能把建议当作已修复。
- D PR #10 新增 `c30f799` 历史 GET 与 LLM 依赖解耦，新组合为上述 f465ee3。相对 119a10c 只改三个 AI 路由/依赖/服务文件、一个测试文件及一份 D 文档；C 核心、MarketDataService、交易日历与固定包均未变。

## 新 SHA 上独立执行

| 检查 | 结果与边界 |
|---|---|
| 项目回归 | 555 passed、compileall 通过；Python 3.12.10，1 条既有弃用提示 |
| 三股×三参数 | 9/9；完整包 direct、实际 ASGI POST、新 engine/连接 MySQL GET 的完整结果/类型/哈希/订单/三曲线一致；每曲线 283 点；九组 direct 与 119a10c 逐字节一致 |
| MySQL 8.0.31 | 独立隔离库 V1→v8、中断恢复、原列/旧记录保全、重复迁移通过；未改原项目库 |
| 离线 AI 隔离 | 7 项通过，合成行情/假 LLM/SQLite；不代表真实 LLM |
| 新历史依赖 | 空 LLM 配置，实际 ASGI 路由+MySQL；禁止构造生成/LLM/行情/上下文依赖后，合成 legacy 报告列表/详情均 200；缺失报告 404/40006；生成入口缺配置 500/50005；重连后全部 AI 行不变 |
| 运行时定向探针 | SQLite/MySQL 各 10 场景，8 项满足、2 项不满足；正反切源/同源重定基、原子失败回滚、正常完成日路径通过；两处旧阻塞仍复现 |

历史依赖用另建的完整合成 legacy 报告，未调用真实 LLM。最初复用了迁移测试中只填部分列的哨兵记录，它缺必填报告正文，不能用于 API 合法报告测试；已保留哨兵不变、另建合法样例后重跑，未把夹具问题记作产品缺陷。冻结快照校验与篡改拒绝由另七项离线检查覆盖；没有宣称完成两份真实生成报告的重启回读或浏览器流程。

## 仍未通过

1. 旧缓存 09-16/09-18，缺 09-17，完成日上限为 09-17。union 仍把 fetch_end 扩到 09-18，实际返回/落库包含未完成行。
2. 旧缓存 01-06/07/08，请求 08/09，已注入日历但 union 覆盖返回 None。较短新批次仍替换成功，旧 06/07 日丢失。

两项在新的 f465ee3 上重新执行，不只沿用旧结论。MySQL 原子回滚再次通过；B 已接受无需为“无人验证”重复做。复现详情见 [119a10c 报告](C_V2_119A10C_REVIEW_20260922.md)，本轮结果见以下证据。

## 对 B 修复建议的确认

C 接受“完成日 cap 贯穿 union/返回/发布”以及“expected=None 时保守拒绝”的方向，口径不变，无需再等 C 授权。

- cap 建议在同一次请求确定后传递，避免 query/sync 分别取时钟产生不同边界；日线过滤后再检查 min_rows，检查/写入/返回采用一致的有效集合。
- 注入的 callable 返回 None 必须失败，保留上一成功数据/来源/范围/时间；直接 sync 入口也应明确采用注入的日历或其无法证明时的拒绝策略。任意一项保护条件失败就拒绝，不是必须两项同时失败。
- 旧窗口首尾保护可作额外检查，不能替代可信交易日历对中间缺日的完整性判定。清除 cap 之外的未完成脏尾不能被误当成历史截断。
- 回归必须含 Provider **严格遵守请求范围**的 C 原场景；可另外加入 Provider 越界返回的防御场景。断言返回值、实际落库、成功元数据以及失败后的旧快照，而非只检查行数/退出码。

D 是当前实现/集成方，B 独立复审；请沿该安排落地，若 D 委托 B 接手再由 B 提交，不并发改同一实现。C 本轮只发布证据与契约反馈，不修改他人生产代码。

请 D 在 PR #10 合并门槛显式列出上述两处缺陷，避免仅写“等待 C 签字”掩盖尚未修复的实现问题。当前九组和历史依赖可以按本 SHA 记录通过，运行时保留未通过；修复产生新组合后再复验，真实三股/LLM/关 Mock 页面仍按原分工验收。

## 证据

[汇总](evidence/c-f465ee3-20260922/summary.json) · [九组/迁移](evidence/c-f465ee3-20260922/mysql-acceptance.json) · [历史依赖](evidence/c-f465ee3-20260922/history-dependency-probe.json) · [MySQL 运行时](evidence/c-f465ee3-20260922/runtime-mysql.json) · [SQLite 运行时](evidence/c-f465ee3-20260922/runtime-sqlite.json) · [源码/包](evidence/c-f465ee3-20260922/source-audit.json) · [离线 AI 隔离](evidence/c-f465ee3-20260922/AI-compatibility.json)

通用 SQLite 复现：`python docs/evidence/c-f465ee3-20260922/runtime_probe.py --source CANDIDATE_CHECKOUT --output NEW_OUTPUT_JSON`。退出 0 表示诊断完成，必须读取 `all_contracts_met`；目前为 false。MySQL/历史辅助探针依赖 C 本机私有配置加载器与本轮隔离夹具，其他成员需接入各自隔离数据库；凭据/本机库名未公开。
