# C 对 V2 组合 15315c6 的复核记录

> 历史报告：以下结论仅绑定原组合 15315c6。后续 B9a1、A0108410 已分别通过有关修复；当前状态请看 [C 项目进度看板](C_PROGRESS.md) 与 [后续复验记录](C_V2_FOLLOWUP_REVIEW_20260916.md)。不将后续通过结论追溯套用到本旧 SHA。

日期：2026-09-16。被测组合为 `27ye/ai-quant-platform@15315c6fbf9339694eceaab0172d3d3f05e7ec2e`。C 核心 12 个文件与已发布 `92df017404126e9af920e1ad82e4a136d813f8d1` 逐字节一致。组合源归档的 219 个文件未改动。

**结论：C 直接计算、服务返回及 SQLite 快照复验通过；真实 MySQL 完整结果精确回读与历史列表尚未通过，当前不能签发最终全链路验收。** 本轮只新增 C 的验收证据与协调记录，不修改算法、A/B/D 生产代码，不合并 PR。

## 已独立通过

- 组合完整后端测试 477 项，compileall，通过；前端 vue-tsc + Vite 构建通过。已有 Starlette/AnyIO 弃用提示和 Vite 大包体提示不影响这些检查。
- C/D 兼容脚本 7 项通过：默认 AI 上下文使用默认 C 配置，自定义回测不污染默认上下文；快照哈希可复现，历史只读无重算，损坏快照被拒绝。此脚本使用合成行情、假 LLM 和 SQLite。
- 冻结 V1 的两份完整直接计算 JSON 与原基线相等：评分 33，订单记录 24 条，买卖往返 12 次，权益曲线 403 点；实际 HTTP 评分/默认回测、SQLite 新会话历史曲线与订单复验通过。V1 摘要资金为 2 位、收益为 8 位；V1 未保存完整 V2 `c_result`，不作该项全量相等声明。
- 三股 `600519 / 000001 / 300750`，固定窗口 `2025-07-04 ~ 2026-08-31`，参数 `{}`、10/30、20/60+资金200000+成本0.002+滑点0.001：9 组 POST 中的 C 结果字段与直接计算一致，顶层 `effective_parameters` 按五字段契约单独比较。SQLite 关闭连接池并重开后 GET 完整 `c_result` 与直接计算精确相等，每组区间曲线 283 点。
- 六份输入文件原字节符合独立确认哈希。旧包 `600519` 的 `2026-09-15` 六字段差异仍未消除；该日在本次固定窗口之外，不能以九组通过推导整个 436 行包已通过。
- A 的 43 个前端 Git blob 与 `d8af7b4ad8552d0bce8a4c9c2dc145dd2bc0a096` 相同；A2/A3 已集成。17 条参数日期、8 条 AI 历史和 8 条回测历史过期请求探针通过。

## 新增真实 MySQL 复验及阻塞

本机 MySQL **8.0.31**，新建唯一 `ai_quant_c15315_...` 验收库，保留证据；没有写入已有 `ai_quant` / `ai_quant_test`，没有改变服务器或会话参数。新库迁移至 schema v7，再跑迁移版本行不变；这不是 V1 升级验收，D 已提交的 MySQL 8.0.41 升级证据属于 D 的环境。

三股各 436 条规范化静态 JSON 入 `stock_daily`、提交后新 engine/session 仓储回读，全部字段一致；以该回读行情进入九组真实 FastAPI/Service/C 计算，九次 POST 成功并与直接计算一致。本次未访问实时 provider 或真实 LLM，不是 B 的新导出批次，也不是远端部署验收。

### B：历史列表真实返回 50002

九条完整回测记录保存后，默认 `GET /api/v1/backtests`、筛选 `stock_code=600519`、`page_size=1` 均返回 HTTP 500 / `50002`。原始数据库错误为 `1038 Out of sort memory`。会话 `sort_buffer_size=262144`，`EXPLAIN FORMAT=JSON` 显示 `using_filesort=true`，实际查询选择了整个 `BacktestResult`，包含大 JSON。

同库、同排序、同缓冲区的只读摘要投影诊断可以返回 9 条，顺序 `9..1`；没有将诊断查询替换进生产代码。`backtest_service.py` 在组合与 B 最新 `2871253` 整文件相同，因此 B 最新分支也仍有该查询。

请 B 把列表查询收敛到必要摘要字段和轻量快照状态，保留 `created_at DESC, id DESC`、过滤、count 与分页语义。避免简单 `load_only` 后又由 `_to_summary().equity_curve` 触发懒加载。状态需覆盖 SQL NULL、JSON null、空数组、非空数组。结合查询计划评估索引；不以全局增大缓冲区替代修复。用真实 MySQL 九条完整结果做回归。

### B/C：完整 c_result 的 MySQL 精确回读不满足契约

新会话按 ID 查询的九个详情均 HTTP 200，但 **0/9** 完整 `c_result` 与保存前对象逐字段精确相等。共 1439 处差异，全部为数值叶子的 1 ULP 末位变化，最大绝对差约 `2.91e-11`；例如基准权益 `99633.35582084299` 回读为 `99633.355820843`。输入快照逐字段相同、输入哈希重算通过，订单次数/日期/参数/买卖方向相同。它不会造成肉眼可见的收益变化，但不满足既定原样快照契约，不能借扩大容差宣告通过。

只读 MySQL SQL 已隔离复现，不依赖算法或页面：

```sql
SELECT CAST('{"number":99633.35582084299}' AS JSON);
-- 本机返回 {"number": 99633.355820843}
```

三个取自实际结果的数值均复现；将原始 JSON 文本作为字符串封装再读取的对照则可保持文本及 Python 解码值完全一致。本结论限定于本机 MySQL 8.0.31 当前 JSON 转换/回读路径，不推论所有版本必现，也不据此裁定变化具体发生在内部存储的哪一阶段。这仅为无损保存方向的验证，不是已实施的生产修复。

请 B/D 确定无损保存完整 C JSON 文本的落库与旧记录兼容方案，API 对 A/C 继续返回原结构对象。历史完整结果、曲线与订单应来自冻结快照；不要修改 C 算法、全局舍入或把所有数值转字符串传给前端来掩盖存储变化。摘要 DECIMAL 的约定舍入不属于此问题。

## A：回测面板请求状态交接

对原始 `BacktestPanel.vue` 在内存编译后控制 API Promise，普通 V1→V2 顺序流程通过，以下三条已复现；这不是浏览器端到端测试。

1. V2 等待期间切股：新股票加载完成，但旧请求留下 `submitting=true`，提交按钮一直加载。
2. V1 初载未结束就提交 V2：V2 成功但 `loading=true`，持续显示骨架。
3. V1 初载失败后 V2 成功：`failed=true` 未恢复，仍显示失败。

请 A 在当前请求接管面板时统一处理 `loading/failed/submitting`，同时保留过期请求保护；三条路径补回归后交 D 集成。`BacktestDetailView` 的“结果哈希”应改成“C 输入快照哈希”，因为 `c_data_hash = input_snapshot.sha256`。

## B 新导出版本的独立状态

B 最新 `2871253240edb3d0ecc8ade946a754b2326a9274` 包含 `43269f5` 一致性修复；组合 `15315c6` 中对应两个脚本仍与 B85 相同。两份版本分别裁定。

- B 新版针对性测试 22 项通过；C 原五个反例均修复：正确舍入通过、错代码/重复日期/NaN 失败、双可选 null 通过。README 哈希说明已更正。
- 新单源导出、canonical 舍入、提交后新会话回读基本路径成立。
- 实际 CLI 仍允许同批目录重复执行并覆盖文件；非空重试写库失败后，进程异常退出，但旧 `status=ok` 清单留存且 hash 与新文件不符。请在任何副作用前拒绝已存在目录，或用完整 staging/原子发布方案，并补失败清单。
- `--last-complete-trading-day` 尚不约束实际行；边界写 9/15 仍可导出 9/16 并 `BATCH OK`。请校验并在写文件/写库前拒绝或按约定截断，记录日期依据及排除情况。

上述两个导出问题属于新数据包交付门槛，不是旧组合应用回归。尚无新真实数据包可验收，原整包差异仍保留。

## 后续验收顺序

B 修复真实 MySQL 列表与无损快照，A 修复面板状态；D 集成并给出新完整 SHA。C 对新 SHA 复验受影响路径，保留 V1、九组数值及默认 AI 兼容检查。B 的新数据包单独按新批次 SHA/哈希复验。只有届时通过，才发布最终全链路验收结论。

## 可共享的证据

- [版本、输入哈希、迁移/测试与 MySQL 查询计划汇总](evidence/c-combined-15315-20260916/summary.json)
- [九组 MySQL 精度与输入哈希核对](evidence/c-combined-15315-20260916/mysql-precision.json)
- [只读 JSON 数值对照](evidence/c-combined-15315-20260916/mysql-json-number-probe.json)
- [回测面板状态探针](evidence/c-combined-15315-20260916/panel-state-probes.json)
- [B 原五个反例复验](evidence/c-combined-15315-20260916/b-checker-probes.json)
- [B 新导出 CLI 复现](evidence/c-combined-15315-20260916/b-export-cli-probes.json)

共享材料只含项目样本、版本及检查结果，不含连接密码、密钥或本机用户目录。
