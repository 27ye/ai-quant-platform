# C 对 B25c 搜索状态修复的复验

日期：2026-09-16。B 完整 SHA：`25c11aa7b55d67fa5f77b3c603ca84b5369dc48e`。原回复：[Issue #11 / 5694407714](https://github.com/27ye/ai-quant-platform/issues/11#issuecomment-5694407714)。当前总进度见 [C 进度看板](C_PROGRESS.md)。

**结论：此前“目录成功同步后刷新失败，搜索却返回50006”的问题，在 B25c 上复验通过，可以关闭。** A 的50006中文映射仍待提交；最终组合验收仍等待D。

## 本轮实际验证

- 精确 B 源码完整测试：**359 passed、1条既有 Starlette/AnyIO 弃用警告**；Python 3.12.10；`compileall backend scripts tests` 通过。200个归档原始文件在测试前后哈希不变。
- 用固定的1000条合成目录和内存SQLite调用实际FastAPI路由、服务、仓储；以下7个场景通过，所有搜索GET的provider调用均为0。

| 场景 | HTTP / 业务码 | 结果 |
|---|---|---|
| 从未同步 | 503 / 50006 | 明确未同步，不取行情 |
| 首次同步失败 | 503 / 50006 | 不伪装无匹配 |
| 已同步、命中 | 200 / 0 | 返回600519 |
| 已同步、无匹配 | 200 / 0 | 空数组 |
| 已同步、查询数据库异常 | 500 / 50002 | 保留数据库错误语义 |
| 空关键词 | 400 / 40001 | 参数错误 |
| 成功同步后刷新失败 | **200 / 0** | **仍返回600519**；status=failed、last_error保留、last_success_at不变、1000条目录保留、可用性为true |

四处文档/注释（stocks.py、dependencies.py、API_SPEC §4.1、V2_B_DATA_CONTRACT §3.3）已同步本地查询与三种状态语义。[完整API证据](evidence/c-b25c-20260916/coldstart.json) / [测试摘要](evidence/c-b25c-20260916/summary.json)。

## 与前轮证据的关系

相对 B9a1，仅7个文件变化：目录服务、两个API文件的说明、两份文档和两份测试，无文件增加/删除。回测、持久化、迁移、导出代码均未变，见 [文件比较](evidence/c-b25c-20260916/source-comparison.json)。

因此本轮聚焦新增搜索修复，没有重复跑真实MySQL整链路。此前旧九条标记、新九组无损回读、列表、8项导出探针的结论仍绑定 **B9a1 + C92df017** 的实际被测版本，见 [前轮记录](C_V2_FOLLOWUP_REVIEW_20260916.md)。不能把它们改称“B25c已做新的MySQL验收”。

本轮没有真实provider/LLM、V1→v8、远端部署、前端构建/浏览器或D最终组合验证。C量化核心、A/B/D生产文件均未修改。

## 剩余协调

- **A**：远端核对仍为 `010841099b20b500ed322b15cec32d9e7add97cd`，`frontend/src/api/http.ts` 尚无50006中文映射；这是静态核对，不冒充新前端运行验证。[检查记录](evidence/c-b25c-20260916/a-interface.json)。请补映射并验证HTTP503下的提示；已有目录刷新失败返回200，应保持成功页面。
- **D**：核对PR #10仍为 `15315c6fbf9339694eceaab0172d3d3f05e7ec2e`。集成当前B/A修复后，完成V1→v8、旧历史、重复迁移与实际页面/API验证，给出新完整SHA。C随后复验最终组合。
- **数据包**：新真实包仍未交付；600519的2026-09-15旧包差异继续单列，不因代码修复通过而宣布整包通过。
- **既有业务口径**：目录完整性按≥1000条还是与上游total一致，仍由B/D另行确认；本次未修改，也未将其新增为本轮修复的阻塞。文档中把一次34秒测量扩大为“永远不可能成功”的措辞可改为该环境的实测结论，不影响本次功能通过。
