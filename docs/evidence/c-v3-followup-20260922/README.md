# C 最新通知逐项核对

日期2026-09-22。本轮处理B comment5777859232、B新PR24、A23新提交；不新增算法、不重复签最终验收。

## B回复中的状态纠正

B已提供main10283b0的真实MySQL迁移检查及D22 fedeb09公共错误码核查。这些是B执行的证据，C接收其声明，不标为C亲自运行。C之前的新组合746 passed/1 skipped是SQLite范围，二者不可混写。

该回复第2/3项将D状态描述为“零资金仍待修、仍基于旧main、v9仍是D自己的实现”，与相同完整SHA的Git对象不符。独立核对：

- `git merge-base --is-ancestor 10283b079f686cb8310bf18125d7992b60979293 fedeb09d05b9073e30af50b202b94f40ec30d789`退出0；merge-base就是10283b0。D已rebase到B19/B21合入后的main。
- `git diff --exit-code 10283b0 fedeb09 -- backend/app/db/migrations.py`退出0。
- 两版本该文件Git blob都为`8a5a3eda3df0d93a9fbccb75fa08b73a2cafa85d`，不是仅凭函数同名或数量判断。
- 两版均保留B的五个test_v9_*专项用例；test_db_migrations.py仅额外在既有V1升级断言中包含analysis_mode/backtest_id两列，不存在D原两条重复v9专项用例。
- 文件里存在一份_migration_v9是正确状态，不能删除B唯一迁移。D去除的是重复实现，不是所有v9函数。
- D生产6c9ca52（头fedeb09）零资金保护已由C上一轮独立验证，四项损坏均JSON50002/LLM0/报告0；review5279080719已APPROVE。无需B继续等待同一修复。

## B PR24

- head `27f83479d56f965901c1eb86e335a5f213b1d72b`，目标main10283b0。
- 以PR merge-base比较：`git diff --stat upstream/main...upstream/pr-24-head`只有tests/test_f3_detail_contract.py +29。不能用两个分支端点的双点差异误判为删除B19文件。
- 这就是C上一轮已获取并定向执行3 passed的同一提交，没有新代码变化，本轮不重复跑相同测试。原生产代码批准保持，可批准本测试PR的C字段口径审阅。
- 测试注释中将实际结束日期误写为2025-08-28；正确示例为2026-08-28（实际断言使用另一组June样本，不受影响）。作为非阻塞文字修正告知B，不修改B代码。

## A PR23新提交

- 新head `af5e17217b16e8270107e771508d378e0b7dbba6`，对旧6daa211有28个前端文件变化。
- C相关只读核对：回测参数提交/收益率换算未变；曲线增加容器resize；评分null分项改为“—”，符合不伪造0分口径。
- 其他改动包括移动导航、搜索/股票名称获取、失败重试、键盘交互、404、主题/动画。它们属于A前端体验范围，未由C进行完整浏览器/可访问性验收。提交标题的“体验修复C包”是A的改动批次名称，不是C量化算法修改。
- **原区间映射问题仍存在**：将之前真实B GET字段输入当前A原始infoRows函数，仍显示请求=2025-07-07至2026-08-28、实际=—；正确请求为2025-07-05至2026-08-30，实际为2025-07-07至2026-08-28。结果为a23-projection.json，执行脚本为probe_a23.cjs。无需B新增actual_*。
- C保持A23既有修改请求，告知新UI提交未覆盖该缺陷，不把界面更新当作数值契约已通过。

## 其余进展

C18与D22仍待D安排集成；D22的C审阅已过，A/B剩余门禁由对应成员确认。最终组合SHA及C4尚未签字。其他通知是既有评审/合并提醒，没有新增C开发事项。
