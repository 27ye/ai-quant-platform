# C：群内协同与 GitHub 新回复处理（2026-09-22）

## 被测版本与范围

- A PR23：`af5e17217b16e8270107e771508d378e0b7dbba6`，原样前端。
- D PR22：`fedeb09d05b9073e30af50b202b94f40ec30d789`（包含 main `10283b079f686cb8310bf18125d7992b60979293` 的 B19/B21）。隔离叠加 C 生产代码 `6a84901c82256b7d48dfab3ebee3208633e5058c`。
- B 新 PR25：`976bb2306dad6b94d24d5842a921332ad92326ae`，另一个干净 checkout 定向测试。
- 数据：既有 `c-delivery-20260917` normalized 腾讯 qfq 历史包。回测使用真实 HTTP/schema/service/C/repository 和临时 SQLite。其他前端接口使用 mock（包括股票名称、K线、评分、数据状态、AI）；未调用真实 LLM。页面上的这些 mock 数据不能作为实时数据证据。
- 这是隔离的页面/接口预验，不是已提交的组合 SHA，不是最终 C4，不包含真实 MySQL、实时 Provider 或最终页面全量验收。

## 本轮执行结果

使用浏览器从工作台展开参数、选择 MACD、填写日期并点击运行，再从历史列表打开详情、勾选两条进行对照。没有改 A/B/D 生产文件。

请求区间均为 **2025-07-05 ~ 2026-08-30**，股票 600519，实际交易区间 **2025-07-07 ~ 2026-08-28**。

| 页面操作 | 核实结果 |
|---|---|
| MACD 日期留空运行 | 提示请选择回测起止日期，没有新增 POST |
| 默认 12/26/9，资金100000，成本0.100%，滑点0 | 实际 POST 六字段，成本0.001、滑点0；200 |
| 快线30、慢线26 | 提示快线必须小于慢线，没有新增 POST |
| 6/13/5，成本0.200%，滑点0.100% | 实际 POST 成本0.002、滑点0.001；200 |
| 历史列表→详情 | 六参数和页面数值与保存结果一致 |
| 历史详情/对照 GET | 记录到9次历史 GET，均200，行情调用0 |
| 完整快照重放 | 两条完整 C JSON（含类型、哈希、订单、三曲线）重放一致 |
| A 原样前端 typecheck/build | 通过；仅构建大chunk提示 |
| B25 新入口测试 | 4 passed，SQLite/fake provider；未重复声称运行B全量592项 |

| MACD | 总收益 | 年化 | 最大回撤 | 夏普 | 胜率 | 往返 | 订单 | 曲线点 |
|---|---|---|---|---|---|---|---|---|
|12/26/9|−17.47%|−15.82%|−26.83%|−1.02|21.43%|14|28|281|
|6/13/5，含上述成本滑点|−18.09%|−16.38%|−20.96%|−1.07|22.73%|22|44|281|

这些数字是指定历史样本的策略回测，不用于证明盈利能力。机器结果、实际请求审计见 `audit.json`、`requests.jsonl`、`backtest-2.json`、`backtest-3.json`。`ui_audit_verify.py`核对程序与临时服务脚本随附；前端操作是人工式浏览器操作，不是该脚本自动生成的。运行核对脚本需已完成相同页面操作、服务仍在运行且数据ID对应。

## 全部协同事项与归属

1. **A→C MACD接UI验收：本轮已执行。** 两组页面提交、参数换算、历史数值均通过。C算法及远端请求契约不变。
2. **A评分null：口径确认。** 原始ScoreCard源码对null使用“—”（附上限），不能当成0分。本轮评分为mock正常数值，没有注入null做真实浏览器空值测试；不宣称C实际评分会在预热期返回null，不据此改变C评分契约。
3. **A F3映射仍待修。** 浏览器 `/backtests/compare?a=3&b=2` 实际显示“请求区间2025-07-07~2026-08-28、实际区间—”。B响应明确请求为07-05~08-30，实际为07-07~08-28。沿用既有修改请求：请求读data_meta.requested_*，实际读顶层start_date/end_date或computed_*；缺历史请求信息如实标记。B24已固定测试，无需新增actual_*。
4. **A→B目录触发：GitHub新增PR25已处理。** 4项定向测试通过（同步解锁搜索、重复刷新、首次失败50001/后续搜索50006、刷新失败旧目录可用）。只说明未初始化目录会50006，不能说所有已有部署目录永远为空。该入口尚待D集成；不是启动自动同步，也不是上游恢复证明。
5. **A/B strategy冻结：确认维持B21契约。** MACD六字段，日期必填；未额外增加接口字段。
6. **A/D导出按钮、旧返回键、路由：由A/D处理。** D已确认保留导出、不恢复旧返回键；custom复用既有/ai/reports/:id，meta.title/scrollBehavior/404属于A router。C无相应生产改动，最终合并仍需D检查。
7. **A环境与视觉事项：记录，不代签。** 实时502/名称fallback/mock开关、主题/动画/移动导航属于A/B/D；本次离线预验不能确认其线上状态。禁用系统减少动画规则是A的视觉/可访问性选择，不属于C算法验收。
8. **B公共错误码：D fedeb09已公布，不再等待同一head。** 已有B对该head的核查按实际范围保留；D若再改公共错误码应复核。C对D零资金等四项损坏50002的复验及APPROVE不变。
9. **D→B主审C18：提醒补审。** B当前“只剩两项”清单应补入D刚要求的C18 MACD接线主审；C不能自批自己的PR。
10. **D最终组合/B真实库/C4：仍待。** D集成C18、A23修复、B24、D22，并决定新B25是否纳入；冻结完整40位组合SHA和数据包后，B跑真实MySQL迁移/历史/报告两列，C跑最终三股三参数及直接计算/POST/重建连接GET一致性。叠加候选与最终版本分别标注。

没有合并任何PR，没有修改其他成员代码，没有将微信截图视作已合入/已验收证据。
