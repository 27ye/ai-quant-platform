# C 对 D PR #22 的独立数值复核

日期：2026-09-22。结论：**正常数值链路通过；保存结果内部矛盾的拦截未通过，请 D 修复后复验。** 不改变 C 算法，不修改 D/B 生产文件，不宣布 V3 整体完成。

## 被测版本与范围

- D PR #22：`bf7cd54a7694728e48937ce9d8f54a5ae4a12714`，原始 checkout 全量 **596 passed**。
- 独立组合：同一 D SHA + C PR #18 的 quant 实现 `6a84901c82256b7d48dfab3ebee3208633e5058c`，加 C 两份测试及离线脚本/样本，全量 **713 passed**（2 条依赖弃用 warning）。这是本地复核组合，不是已发布的最终集成 SHA，不包含 B PR #21。
- Python 3.12.10；真实 FastAPI 路由、上下文校验、Repository；临时 SQLite 文件与重建 engine/session；固定 LLM 假件。未执行真实 MySQL、付费 LLM、实时行情和浏览器验收。D 的相应证据由 D 负责，本记录不改签其范围。
- 固定样本：`docs/evidence/c-delivery-20260917` 三股 normalized 文件，文件 SHA-256 在两个 result.json 中。

## 已通过

1. 600519 / 000001 / 300750 × MA 5/20、10/30、20/60 三参数组。第三组资金200000、费率0.002、滑点0.001，其余沿用默认；区间2025-07-04至2026-08-31，每组283曲线点。
2. 两种代码组合各9组：直接 C 计算 → 精确保存文本 → 原始 context 数值、类型、参数、预热、初始权益、执行假设、输入哈希一致。
3. 独立实现15位有效数字归一化后，与 Prompt / HTTP POST / 保存快照 / 重建连接后历史 GET 完整一致；原始 C 文本没有被归一化改写。
4. 独立 canonical JSON SHA-256 等于 `context_hash`；`data_hash` 保留 C 输入快照哈希，二者没有混用。它们不是结果真实性的同一个证明。
5. 自定义生成的行情/交易日历/默认上下文入口设为禁止调用，历史 GET 另禁止构造 LLM；量化计算入口在生成/历史阶段也设为禁止调用。
6. 组合环境真实 C MACD 保存结果 → 422 / 40007，LLM调用0。符合 F4 首版仅支持 MA 的范围，不能当作 MACD 回测接线失败。

## 需 D 修复：内部矛盾结果仍会付费生成并保存

在一份有效 `300750` / MA20/60 精确结果中，只修改 `c_result_text` 的一个结果字段，保留输入快照及合法输入哈希，三种情况都得到 **HTTP200 / code0、LLM调用1、报告新增1**：

| 单独修改字段 | 原值 | 修改后 | 矛盾证据 |
|---|---|---|---|
| total_return | 0.25458314506235635 | 0.99 | 初始200000，末权益250916.62901247127及权益曲线未改变 |
| max_drawdown | -0.2385075991954868 | -0.5 | 保存的权益/回撤曲线完全未变，摘要不匹配 |
| trade_count | 2 | 999 | 订单仍5条，不能有999次已完成买卖往返 |

C 的 max_drawdown 本来就是非正数；这里的问题是与曲线不一致，不是负数非法。探针在 D 原版及 D+C 组合均复现。它模拟损坏/错误写入的保存结果，并不声称正常回测会产生这种数据。

依据 `V3_DEVELOPMENT_PLAN.md` F4 第2、5条，必要精确快照应可校验，字段矛盾应按数据损坏处理。当前 `_validate_snapshot` 只比末权益和订单条数，没有核对上述结果恒等式。输入哈希本就不涵盖结果，不能承担这项校验。

请在调用 LLM 前，仅根据保存字段校验基本恒等式（总收益与初末权益、回撤摘要与保存曲线、完成往返数与买卖订单），使用明确浮点容差；失败应 **500 / 50002、LLM调用0、成功报告0**。不要为此重新调用 C 回测、取行情、改输入哈希口径或覆盖旧结果。建议将三例纳入 D 自动化测试，并保留正常九组通过。

## 重现

从 D 被测 checkout 执行，探针从当前工作目录加载项目代码；输出写到新文件。需要项目 Python 依赖，禁用真实 LLM 配置，数据库指向不可达的测试地址，脚本实际仅使用临时 SQLite：

```powershell
python <本目录绝对路径>/probe.py <新输出文件.json>
```

叠加 C quant、测试/脚本及 `c-v3-macd-20260922` 样本后：

```powershell
python <本目录绝对路径>/probe.py <另一个新输出文件.json> --with-c-overlay
```

探针正常链路有断言；损坏用例是诊断输出，**exit 0 不代表拦截通过**，必须检查 `corruption_probes` 的实际值与 expected。`d22-result.json` 为原 D 结果，`d22-c-result.json` 为组合结果；不要将本地组合当成远端已合入。

## 远端协同

- D：修复本项后给新完整 SHA，C按同一数据矩阵复验；F4 当前不要求支持 MACD。
- B：PR #21仍为 `11a0c03ecca56fe667b845a976afd1cdde9197b7`。上一轮 MACD日期缺失/null自动补齐、GET实际参数12字段而POST6字段两项仍待新提交，不因D交付自动关闭。
- D/B：PR #22也触及schema v9，与PR #19存在同一迁移职责，请明确合入来源并在最终组合复验，不能把两份分支各自测试当成合并证据。
- A：F4 `custom_backtest` 的quant_score为null、trend为neutral；收益来自保存策略结果，不能显示成当前股票评分。待最终接口版本后完成页面验收。
