# C 对 B19/B21 合入主分支后的兼容预验

日期2026-09-22。**主分支+B19/B21+C量化隔离组合通过；不是最终V3验收。**

## 本次通知的实际内容

- PR19已由D/27ye合并，merge SHA `bb65ce9c9b623d19ee819b56382ebf08506ae6c5`。
- PR21已由D/27ye合并，当前被测main `10283b079f686cb8310bf18125d7992b60979293`。
- 通过GitHub merged=true以及git祖先关系双重核对，B19 `fa91f4ca7477cc28db4df4021e32e40a42870fe6`、B21 `0360b699f6530aefd5afe5e11e859f4c179146d8`都已包含。
- C PR18仍open/未合并；D22仍44ca75a（仅文档），A23仍6daa211。没有新修复可供对应缺陷复验。本次不是B又提出一轮修复请求。

## C已执行

1. 在干净main 10283b0上执行`tests/test_strategy_entry_integration.py`：**4 passed**。MA旧路径通过；当前缺C V3支持时MACD按既定部署缺口拒绝、取数0/落库0。
2. 隔离目录叠加C quant代码`6a84901c82256b7d48dfab3ebee3208633e5058c`及其两份测试、脚本、样本；B生产文件不改。全量 **708 passed, 1 skipped**，2条依赖弃用warning。skip为C已存在后不再执行的“缺C实现”分支测试；探针另强制关闭支持位覆盖50004。
3. 固定三股×三组MACD参数：直接C→真实HTTP POST→重建SQLite engine/session GET完整精确结果一致；六字段实际参数值/类型一致，九组历史参数再次POST成功且结果不变。
4. 五种日期缺失/null全部400/40001、取数0/落库0；六项非法参数保持40001，MA默认窗口保持旧行为；缺C支持500/50004，取数0/落库0。
5. 该组合使用B唯一v9实现（SCHEMA_VERSION=9），全量包含迁移及损失精度防护测试；使用临时SQLite库，不触碰项目MySQL库。

结果及可执行探针为`result.json`和`probe.py`，用法是在上述main+C隔离checkout中执行`python <probe.py绝对路径> <新结果json路径>`。每组283点，数据为既有三股腾讯qfq normalized，参数组与先前B21专项相同。未重新取实时行情。

## 结论和剩余职责

- C实现与B19/B21实际合入主分支后的接口兼容，C算法不需要修改，可供D审阅/集成PR18；此处不自动合并任何PR。
- **main当前还不包含C MACD代码**。不能将B接线合入当作F5已经可用；最终需D集成C18（及A表单）并重新验证最终SHA。
- D22的零初始资金损坏记录应返回JSON50002问题仍待D代码修复；v9来源决定已执行到PR19合入，D22重复迁移去重/rebase尚需D完成。
- A23请求/实际区间映射与F3字段确认仍待A。既有请求不重复新建。
- B既有两项整改已批准并合入，不再等待C对0360b69重复签字。B后续公共错误码核查/真实数据库组合检查由B负责。
- PR22/Issue20通知中显示的是前轮C自身回复；PR13关闭是已知前端整理，其余旧V2提醒没有发现新的C执行要求。

本轮无D22/A23集成、真实MySQL/Provider/LLM/浏览器验收；主分支加C属于候选组合，不是D最终冻结SHA。C4最终签字仍待全部必需门禁。
