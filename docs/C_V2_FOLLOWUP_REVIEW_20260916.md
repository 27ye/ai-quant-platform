# C 后续复验记录：B843 → B11c7 → B9a1 / A0108410

> 后续更新：本文保留 B9a1 轮次的状态。新增搜索边界已在 B25c 修复并通过，见 [最新搜索复验](C_V2_B25C_REVIEW_20260916.md) 和 [进度看板](C_PROGRESS.md)；不继续将本文的旧搜索反例视作 B25c 未修事项。

日期：2026-09-16。当前状态见 [C 进度看板](C_PROGRESS.md)。本次提交整理此前已经执行并发布评论的复验，不声称本轮重新跑了全部产品测试；C 量化核心仍为 `92df017404126e9af920e1ad82e4a136d813f8d1`。

## 各轮结果

| 被测版本 | 独立检查 | 当时结论 | 当前处理 |
|---|---|---|---|
| B `843f513327dfccd98ee1f11a65b1b04b9aaa2ebf` | 完整测试 348；原九行库 8 项列表、9 项详情；CLI 反例 | 列表修复通过；精度未修；完整日可省略 | 作为中间证据保留；后两项见 B9a1 |
| B `11c7ec2973e33bbb1b0d0a52bd793bfde6f3c9b8` + C92df | 完整 B 测试 354；MySQL v7→v8、旧九条、新九组 | 新记录无损通过；旧九条误标 exact=true；日期仍可省略 | 两项均已在 B9a1 关闭 |
| A `010841099b20b500ed322b15cec32d9e7add97cd` | 7 项原始 SFC 受控 Promise/computed 探针 | 普通路径、三条状态路径、三种哈希路径通过 | 待 D 集成后验证真实页面/API |
| B `9a1b9b45235c07b01b0ab30d0fcbb7ad719fea8b` + C92df | 完整 B 测试 357、compileall；MySQL 新旧18条；8导出探针 | 旧 exact 与日期必填两待修均通过 | 等待 D 新组合验收 |
| B9a1 新增搜索协议 | 7 个隔离 SQLite 实际 API 场景 | 六项正常/预期异常契约成立；另复现“成功后刷新失败仍50006” | B/D 对齐状态，A 补消息映射 |

348/354/357 是各 B 源码版本的完整测试数；早期 D15315 的 477 是组合源码测试数，不能相加，也不拿数量变化推断删减或覆盖更完整。

## B9a1 的真实 MySQL 复验

在本机 MySQL 8.0.31 的新独立库，保持 `sort_buffer_size=262144`。从原 C v7 验收库只读复制九条旧记录及版本表，原库不变；生产迁移到 v8 并回填文本列。

- 旧九条历史内容与原响应保持一致，均正确 `c_result_exact=false`。旧 JSON 丢失的浮点末位没有被伪造恢复。
- 新三股九组：600519、000001、300750；窗口 2025-07-04..2026-08-31；默认参数、10/30、20/60+资金200000+成本0.002+滑点0.001。直接计算与实际 POST 的 C 字段一致，顶层五参数投影单独核对；新 engine/session 的 GET 完整 `c_result` 数值及类型精确相等，均 exact=true。
- 新旧18条下7项列表检查均成功，每次2条SQL，不投影完整文本；重复迁移不改变版本行、新旧快照指纹。

B 原始源码归档未改；运行副本仅叠加已发布的 C 量化文件，不将此称为 D 发布的最终组合。输入文件哈希、九组结果概要及查询数见 [mysql-v8.json](evidence/c-followup-20260916/mysql-v8.json)。

## 导出与 A 定向检查

8 项 [导出探针](evidence/c-followup-20260916/export-cli.json) 使用固定 provider 和内存 SQLite，实际执行生产 CLI/函数。缺少完整日时 CLI 返回2、无输出目录、无取数/入库/SQL；函数层同样拒绝。有效输入、批次不可覆盖、越界拒绝、失败清单及失败批次重试保护通过。不证明提供的日期就是现实中的最后完整交易日，也不替代新真实数据包验收。

A 的 [7 项证据](evidence/c-followup-20260916/a-panel-state.json) 验证普通 V1→V2、V2 在途切股、V1 在途提交V2、V1失败后V2成功，以及输入哈希优先/回退/缺失。三条原故障结束后 loading/failed/submitting 均清理，旧响应未覆盖当前结果，标签为“C 输入快照哈希”。这些是原 SFC 内存编译与受控状态探针，没有冒充浏览器或真实接口。

## 仍需对齐的内容

1. **B/D 搜索状态**：[冷启动证据](evidence/c-followup-20260916/coldstart.json) 表明，无成功目录时503/50006、已同步命中/无匹配、数据库错误50002、参数错误40001均成立，GET provider调用为0。但成功同步1000条后刷新失败，目录和成功时间仍在，搜索仍返回50006，需区分于从未同步并同步旧契约文档。
2. **A 消息映射**：核对A0108410时尚无50006中文映射，需验证真实503响应能展示约定提示。此前状态修复结论不变。
3. **D 最终组合**：最后核对PR #10仍为15315c6；待D集成并验证V1→v8、旧历史、幂等、页面/实际接口后交新完整SHA，C再复验。
4. **数据包**：600519 2026-09-15旧包差异仍未消失；上述固定窗口不覆盖该日，不能据此宣布整包436行通过。新真实批次、hash与独立回读仍另验。

## 可追溯入口

- [B843 C 回复](https://github.com/27ye/ai-quant-platform/issues/11#issuecomment-5693783029)
- [B11c7 C 回复](https://github.com/27ye/ai-quant-platform/issues/11#issuecomment-5693920270)
- [A0108410 C 回复](https://github.com/27ye/ai-quant-platform/issues/11#issuecomment-5693951734)
- [B9a1 C 回复](https://github.com/27ye/ai-quant-platform/issues/11#issuecomment-5694237707)
- [证据摘要](evidence/c-followup-20260916/summary.json)：记录完整SHA、实际环境、原报告及发布文件的SHA-256。原始临时日志留在C本机；发布包使用字段白名单移除本机绝对路径、临时库标识、冗余SQL与生成文件，不包含配置或密钥。
